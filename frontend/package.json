{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-sw.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "lit": "^3.2.1",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "engines": { "node": ">=22" }
}
